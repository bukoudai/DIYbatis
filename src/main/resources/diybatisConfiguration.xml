<?xml version="1.0" encoding="UTF-8"?>
<!-- 主配置文件 -->
<configuration>

    <!-- 配置数据源（连接池） -->
    <dataSource >
        <!-- 配置连接数据库的4个基本信息 -->
        <property name="driver" value="com.mysql.cj.jdbc.Driver"/>
        <property name="url" value="jdbc:mysql:///mybatis?useSSL=false&amp;serverTimezone=CTT"/>
        <property name="username" value="root"/>
        <property name="password" value="root"/>
    </dataSource>
    <!-- 指定映射配置文件的位置，映射配置文件指的是每个dao独立的配置文件 -->
    <mappers>
        <mapper resource="com/zhangxx/dao/IUserDao.xml"/>
        <!--<mapper class="ccom.zhangxx.domain.User"/>-->
    </mappers>

</configuration>